# mpv_r v0.1.0 - Mac Preview

This is the first public Mac Preview of `mpv_r`.

It packages the author's original `mpv_r` review setup as a downloadable macOS app bundle.

## Download

Download:

- `mpv_r-mac-v0.1.0.zip`

Use the attached app zip under Release assets. Do not download GitHub's automatic `Source code (zip)` if you want to run the app.

## What's Included

- `mpv_r.app`
- README and install notes
- mpv license texts and source links
- Renewed `mpv_r` configuration files

## Notes

- macOS only.
- The app is ad-hoc signed but not notarized.
- On first launch, right-click the app and choose `Open`.
- This release bundles mpv `0.39.0` using the unofficial third-party macOS build by stolendata.
- This project is not affiliated with mpv-player or stolendata.

## Next

v0.1.1 is planned as a Windows Preview.

